{
  "name": "claude-tray",
  "version": "1.0.5",
  "description": "Claude usage in your system tray",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev:mac": "electron-builder --mac --dir && open 'dist/mac-arm64/Claude Tray.app'",
    "build:mac": "electron-builder --mac",
    "build:win": "electron-builder --win --x64",
    "build:linux": "electron-builder --linux"
  },
  "build": {
    "appId": "com.eli.claude-tray",
    "productName": "Claude Tray",
    "artifactName": "Claude-Tray.${ext}",
    "afterSign": "scripts/afterSignHook.js",
    "files": [
      "**/*",
      "!dev-scratch/**/*"
    ],
    "asarUnpack": [
      "node_modules/node-pty/**"
    ],
    "extraResources": [
      {
        "from": "pty-wrapper.py",
        "to": "pty-wrapper.py"
      },
      {
        "from": "agy-pty-wrapper.py",
        "to": "agy-pty-wrapper.py"
      }
    ],
    "mac": {
      "category": "public.app-category.utilities",
      "target": [
        "dmg",
        "zip"
      ]
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": ["x64"]
        }
      ]
    },
    "linux": {
      "target": [
        "AppImage"
      ],
      "category": "Utility"
    }
  },
  "devDependencies": {
    "@electron/rebuild": "^4.2.0",
    "electron": "^39.0.0",
    "electron-builder": "^26.0.0"
  },
  "dependencies": {
    "node-pty": "^1.1.0"
  }
}
